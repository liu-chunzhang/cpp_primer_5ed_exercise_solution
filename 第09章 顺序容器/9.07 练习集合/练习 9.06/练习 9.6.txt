与vector和deque不同，list的迭代器不支持<运算，只支持解引用、箭头、递增、递减、==和!=运算。
原因在于这两种数据结构实现上的不同。vector和deque将元素在内存中连续保存，而list则是将不同元素按照链表方式储存，因此前者可以方便地实现迭代器的大小比较（类似于指针的大小比较）来体现元素的先后关系。而在list中，两个指针的大小关系与它们指向元素的先后关系不一定吻合，实现<运算将会非常困难和低效。