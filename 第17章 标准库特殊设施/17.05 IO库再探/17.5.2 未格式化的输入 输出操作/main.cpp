// 本程序用于验证教材中17.5.2节内容的可靠性，并提供感性认知
#include <iostream>
using namespace std;

int main()
{   /*
    char ch ;
    while( cin.get( ch ) )
        cout.put( ch ) ;
    */

    /*
    int ch ;
    while( ( ch = cin.get() ) != EOF )
        cout.put( ch ) ;
    */

    return 0;
}
