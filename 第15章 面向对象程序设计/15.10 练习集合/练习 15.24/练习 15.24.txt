作为基类使用的类应具有虚析构函数，以保证在删除指向动态分配对象的基类指针时，根据指针实际指向的对象所属的的类类型运行适当的析构函数。
虚析构函数的函数体可以为空，即不需要执行任何操作。一般而言，析构函数的作用是清除本类中定义的所有数据成员。如果该类没有定义指针型成员，则使用合成版本即可；若定义了指针类成员，则一般需要自定义析构函数以便对指针成员进行适当的清除。因此，如果有虚析构函数必须执行的操作，则就是清楚本类中定义的数据成员的操作。