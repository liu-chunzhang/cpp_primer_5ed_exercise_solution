D1类内函数fcn的声明可以修改为
	int fcn() override ;
虽然不加override也不影响正确性，但是无疑表达意思更为清楚，是个好习惯。
个人以为变化有两处：
首先，语句
	bp2->fcn() ;
机理变化，虽然还是虚调用，但是将在运行时运行D1::fcn()。
其次，语句
	p2->fcn( 42 ) ;
错误，因为这样D1就没有返回类型是int的fcn( int )的函数了。