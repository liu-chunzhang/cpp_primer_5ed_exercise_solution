静态类型在编译时就已确定，它是变量声明时的类型或表达式生成的类型；动态类型则是变量或表达式内存中对象的类型，直到运行时才知道它指向的到底是基类还是派生类。如果一个变量既非指针也非引用，则其静态类型和动态类型永远一致，但基类的指针或引用的动态类型可能与其动态类型不一致。
比如，
	Quote *pQuote = new Bulk_quote ;
指针pQuote的静态类型是Quote，在编译时已经确定，但其动态类型是Bulk_quote，直到运行时才能知道它指向的时基类还是派生类。