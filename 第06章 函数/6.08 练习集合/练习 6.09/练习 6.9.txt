在当前Windows10环境下，通过快捷键Win+R调用运行快捷栏，输入cmd则进入命令行模式。在此模式下，先将所有待混合编译的文件（只包括所有头文件和源文件，不包括Codeblock20.03下生成的文件类型，诸如cbp文件）置于同一路径，再找对带混合编译的文件路径以后，输入
	g++ -std=c++11 a.cpp b.cpp c.cpp -o test
则将a.cpp，b.cpp和c.cpp进行了混合编译（相应的头文件a.h等必须在同一路径）。