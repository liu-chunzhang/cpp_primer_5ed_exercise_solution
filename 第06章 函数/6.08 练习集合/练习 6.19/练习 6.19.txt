(a)是非法的。函数的声明只包含了一个参数，而函数的调用提供了两个参数。
(b)是合法的。字面值常量允许作为常量引用形参的值，而字符'a'允许作为char类型形参的值。
(c)是合法的。66虽然是int类型，但是在调用函数时可以自动转换为double类型。
(d)是合法的。vec.begin()和vec.end()的类型都是形参所需的vector<int>::iterator，第三个实参3.8可以在函数调用时转换为形参所需的int类型。