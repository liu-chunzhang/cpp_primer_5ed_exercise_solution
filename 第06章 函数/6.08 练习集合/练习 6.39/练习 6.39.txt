(a)第二个声明是非法的。第二个声明的意图是声明另外一个函数，其只接受整型常量作为实参，但是顶层const不影响传入函数的对象，所以一个拥有顶层const的形参无法与另一个没有顶层const的形参区分开来；
(b)第二个声明是非法的。第二个声明的意图是通过函数的返回值区分两个同名的函数，但是这并不可行，因为C++规定重载函数必须在形参列表上有所区别。如果两个同名函数的形参列表都一样，那么即使返回类别不同也不行；
(c)第二个声明是合法的。两个函数是重载关系，其形参类型有区别。