与值传递相比，引用传递的优势有三个方面：
1.可以直接操作引用形参所引用的对象；
2.使用引用形参可以避免拷贝大的类类型、IO库类型或者容器类型对象；
3.使用引用形参可以帮助我们从函数中返回多个值。
基于上述对于引用传递的优点的分析，有一些显然的适合使用引用类型形参的例子：
1.当函数的目的就是交换两个同类型参数的内容时；
2.当对象时string对象时，为了避免拷贝较长的字符串，应该优先考虑使用引用类型；
3.使用IO库对象，只能使用引用类型。
在其他情形，如求阶乘相关的运算（如练习6.3，或者求解组合数之类的）、整数的绝对值（如练习6.5），适合使用值传递方式。