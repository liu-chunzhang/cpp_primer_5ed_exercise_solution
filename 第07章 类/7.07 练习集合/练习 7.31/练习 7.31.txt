本题的关键在于理解类的声明和定义。声明的作用是告知程序类的名字合法可用；定义的作用是规定类的实现细节。
类Y的声明称为前向声明，它向程序中引入了名字X并且指明X是一种类类型。对于类型X来说，此时我们已知它是一个类类型，但是不清楚其到底包含哪些成员，所以它是一个不完全类型。我们可以定义指向不完全类型的指针，但是不能创建不完全类型的对象。根据这一点，即可确定先声明类型Y，再定义只需要定义其指针的类类型X，最后补充Y的定义即可。