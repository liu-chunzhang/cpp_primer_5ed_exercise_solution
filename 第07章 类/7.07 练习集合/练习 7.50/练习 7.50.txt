相关Person类的内容可以见练习7.22。
在练习7.22中定义的Person类含有3个构造函数，因为前两个构造函数接受的参数个数都不是1，所以它们不存在隐式转换的问题，所以无需考虑指定explicit关键字。
Person类的最后一个构造函数Person( istream& in )只接受一个参数，默认情况下它会把读入的数据自动转换成Person对象。我们更倾向于严格控制Person对象的生成过程，如果确实需要使用Person对象，可以明确指定；在其他情形下则不希望自动转换类型的发生。所以应该把这个构造函数声明为explicit的。