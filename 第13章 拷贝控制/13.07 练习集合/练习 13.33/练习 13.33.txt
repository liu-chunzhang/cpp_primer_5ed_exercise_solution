首先，我们需要将给定Folder的指针添加到当前Message的folders集合中。这样，参数类型就不能是Folder，必须是引用类型。否则，调用save时会将实参拷贝给形参，folder.insert添加的就是形参（与局部变量一样在栈中，save执行完毕就销毁）的指针，而非原始Folder的指针；而参数为引用类型，就可以令形参与实参指向相同的对象，而对形参取地址，得到的就是原始Folder（实参）的指针。
其次，我们需要调用addMsg将当前Message的指针添加到Folder的messages集合中，这意味着我们修改了Folder的内容，因此参数不能是const的。