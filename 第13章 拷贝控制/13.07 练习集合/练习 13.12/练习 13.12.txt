题干中这段代码会发生3次析构函数调用：
(1)函数结束时，局部变量item1和item2的生命期结束，被销毁，Sales_data的析构函数被调用2次；
(2)函数结束时，参数accum的生命期结束被销毁，Sales_data的析构函数被调用。
在函数结束时，trans的生命期同样结束，但它是Sales_data类的指针，并不意味着它指向的Sales_data对象的生命期结束（只有delete指针，指向的动态对象的生命期才结束），所以不会调用Sales_data类的析构函数。