在此程序中，都是拷贝构造函数在起作用，因此定义能生成新的序号的拷贝构造函数会改变输出结果，但是这个输出结果可能会超出初学者所想。
试举一例。假设在定义a时使用默认构造函数将其序号定为0，则在定义b、c时使用的拷贝构造函数由于给每个对象生成唯一的序号，所以b和c的序号分别被设定为1、2。
而在调用f时，由于参数是numbered类型（而并非numbered类的引用），所以会触发拷贝构造函数，使得每一次都将形参s的序号设定为新值，从而导致三次的输出结果为3、4、5。