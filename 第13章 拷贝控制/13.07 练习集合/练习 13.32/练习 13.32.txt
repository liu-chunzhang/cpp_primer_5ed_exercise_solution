就教材正文中出现的类指针版本的HasPtr类进行分析。
默认swap版本简单交换两个对象的非静态成员，对HasPtr而言，就是交换string指针ps、引用计数指针use和整型值i。可以看出，这种语义符合一般对于交换函数的期望——两个HasPtr指向了原来对方的string，而两者互换之后，各自的引用计数是不变的（都是-1再+1），因此，默认swap版本就已经能够正确处理类指针HasPtr的交换，即使制造专用swap版本也不能获得更多收益。