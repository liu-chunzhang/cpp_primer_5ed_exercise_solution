当map中没有给定关键字时，insert操作与下标操作+赋值操作的效果类似，都是将关键字的值添加到pair中；但当map中已有给定关键字，也就是新的转换规则与一条已有规则要转换同一个单词时，两者行为不同：下标操作会获得具有该关键字的元素（也就是已有规则）的值，并将新读入的值赋予它（也就是新读入的规则覆盖了容器中已有的规则）；但insert操作在遇见关键字已存在的情况则不会改变容器内容，而是返回一个值指出插入失败。因此，当规则文件中存在多条规则转换相同目标（单词）时，下标+赋值的版本最终会用最后一条规则进行文本转换，而insert版本则会用第一条规则进行文本转换。