(a)的原意是把getPtr()得到的指针赋值给p，然后去判断p是否是一个空指针，但是这个表达式的意思是先判断getPtr()的返回值是否是空指针，然后将判断得到的bool值赋值给p，然后用p的值判断if语句是否成立。想要恢复为原意，可以做如下修改：
	if( ( p = getPtr() ) != 0 )
(b)的原意是判断i的值是否是1024，但上述表达式实际上是把1024赋值给i，然后以i作为if语句的条件。因为所有非0整数转换成bool值时都对应true，所以该条件是必真。
注：赋值运算符是默认返回所被赋值的左值对象的！