写成下面形式即可用if语句实现判断
 a > b && b > c && c > d
切勿写成
a > b > c > d
因为逻辑运算一旦进行，就会产生bool值。如果只将第二个式子放入if语句中作为判定条件，则a和b比较大小后产生一个bool值（1或0，分别对应a>b和a<=b情况），这个bool值（1或0）接着和c比较，所得仍未bool值；这个bool值再去和d比较，生成一个新bool值。这显然与原来目的背道而驰。