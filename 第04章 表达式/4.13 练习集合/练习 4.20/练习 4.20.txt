(a)合法。先解引用获取当前iter所处位置的对象内容，然后执行自增操作，将迭代器的位置向后移动一位；
(b)非法。先解引用iter得到vector对象当前的元素，结果是一个string，不能够执行自增操作；
(c)非法。迭代器本身没有empty成员；
(d)合法。将iter解引用后得到迭代器当前所指的元素，结果是一个string，显然字符串可以判断其是否为空，可以调用其empty成员；
(e)非法。将iter解引用后得到的string不能够执行自增操作；
(f)合法。将iter解引用，得到其当前位置的对象内容string，取引用的empty成员（判断其是否为空），之后将迭代器iter向后移动一位。