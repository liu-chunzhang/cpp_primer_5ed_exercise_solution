该赋值语句是非法的，虽然连续赋值的形式本身没有错，但是参与赋值的几个变量类型不同。其中，dval是双精度浮点型变量，ival是整型变量，pi是整型指针。
自右向左分析赋值操作的意义，pi = 0表示pi是一个空指针，接下来ival = pi试图将整型指针的值赋给整型变量，这不符合语言规范，无法编译通过。
一种合理的修改方法如下：
double dval ; int ival ; int *pi ;
pi = 0 ;
dval = ival = 0 ;