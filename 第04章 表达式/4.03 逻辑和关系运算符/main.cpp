// 本程序用于验证教材中4.3节内容的可靠性，并提供感性认知
#include <iostream>
#include <vector>
using namespace std;

int main() {
    /*
    vector<string> text{ "Hello!" , "" , "Welcome to C++ family!" } ;
    for( const auto &s : text ){                                       // s被声明为对于常量的引用，因为text的元素是string对象，可能非常大，所以
        cout << s ;                                                     // 将s声明为引用类型可以避免对于元素的拷贝；又因为不需要对string对象进行
        if( s.empty() || *( s.end() - 1 ) == '!' )                     // 写操作，所以s被声明为对v常量的引用。
            cout << endl;
        else
            cout << " " ;
    }
    */
    return 0;
}
