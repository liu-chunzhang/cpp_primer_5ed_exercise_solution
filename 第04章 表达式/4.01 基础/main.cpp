// 本程序用于验证教材中4.1节内容的可靠性，并提供感性认知（由于下面分立的三小节的代码太少，合为一节）
#include <iostream>
using namespace std;

int main() {
    /*
    int i = 6 + 3 * 4 / 2 + 2 ;
    cout << i << endl;                                 // 打印结果是14
    */

    /*
    cout << ( 6 + 3 ) * ( 4 / 2 + 2) << endl;         // 此例说明括号无视优先级和结合律
    cout << ( ( 6 + 3 ) * 4 ) / 2 + 2 << endl;
    cout << 6 + 3 * 4 / ( 2 + 2 ) << endl;
    */

    /*
    int i = 0 ;
    cout << i << " " << ++i << endl;
    */
    return 0;
}
