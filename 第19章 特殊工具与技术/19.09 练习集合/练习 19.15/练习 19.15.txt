首先，区别在于指定指向成员函数的指针类型时，除了给出成员本身的类型之外，还必须给出成员函数所属类的类型并指明成员函数是const成员或者引用成员。比如，指向int型数据的普通数据成员的指针为int*，指向“不带参数并返回int型值的函数”的普通函数指针的类型为int(*)()，而指向“C类的不带参数并返回int型值的const成员函数”的函数成员指针的类型为int (C::*)() const。
其次可能存在的区别是，在成员函数和指向该成员的指针间不存在自动转换规则（教程P221中有论述：当把函数名作为一个值使用时，该函数自动转换成指针），但是在目前的编译器上，这一点是错误的。