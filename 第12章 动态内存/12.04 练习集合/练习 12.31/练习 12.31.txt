就这个问题而言，vector更好。因为虽然vector不会维护元素值的序而set会维护关键字的序，但注意到我们是逐行读取文本，因此每个单词出现的行号是自然按升序加入到容器中，不必特意用关联容器保证行号的升序。而从性能角度，set是基于红黑树实现的，插入操作时间复杂性为O(lgn)（n为容器中元素数目），而vector的push_back可达到常量时间。另外，一个单词在同一行可能出现多次。set可自然保证关键字不重复，但是这对于vector不成为阻碍，只要每次添加行号前与最后一个行号比较一下即可，总体性能仍然是vector占优。