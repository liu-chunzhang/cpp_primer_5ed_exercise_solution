从程序片段看，可以推测程序员的意图是通过new返回的指针值来区分内存成功或失败：成功则得到一个合法指针，转换为整型是一个非零值，可转换为bool值true；分配失败则得到一个空指针，其整型值为0，可转换为bool值false。但普通new调用在分配失败时抛出一个异常bad_alloc，而不是返回nullptr，因此程序不能达到预期目的。可将new int改为new( nothrow ) int来令new在分配失败时不抛出异常而是返回nullptr。这也不是一个好办法，应该通过捕获异常或是判断返回的指针来返回true和false，而不是依赖类型转换。