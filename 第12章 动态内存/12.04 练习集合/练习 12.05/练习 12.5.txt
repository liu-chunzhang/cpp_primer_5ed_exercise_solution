未编写接受一个初始化列表参数的显示构造函数，意味着可以进行列表向StrBlob的隐式类型转换，也就是在需要StrBlob的地方（如函数的参数），可以使用列表进行替代，而且可以进行拷贝形式的初始化（如赋值），这增加了程序的灵活性。
但这种隐式类型转换不总是好的。例如列表中可能并非总是合法的值；再如，对于接受StrBlob的函数，传递给它一个列表，会创建一个临时的StrBlob对象，用列表对其初始化，然后将其传递给函数，当函数完成后，此对象将被丢弃，再也无法访问。对于这种情形，我们可以定义显式的构造函数，禁止隐式类类型转换。