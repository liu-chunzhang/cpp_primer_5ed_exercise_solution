指针p作为if语句的条件时，实际检验的是指针本身的值，即指针所指的地址的值。如果指针指向一个真实存在的变量，其值必然不为0，此时条件为真；如果指针没有指向任何对象或者是无效指针，那么对于p的使用将引发不可预计的结果。
接引用运算符*p作为if语句的条件时，实际检验的是指针所指对象的内容，在上面的示例中是指针p所指的int值。如果该int值为0，则条件为假；否则只要该int值不为0，则对应条件为真。
一个简单的实例代码也附在本节中可供实践。