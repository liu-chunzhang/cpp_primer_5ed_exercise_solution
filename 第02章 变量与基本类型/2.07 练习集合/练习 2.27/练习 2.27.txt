(a)非法。因为非常量引用不能引用字面值常量0；
(b)合法。p2是一个常量指针，p2的值永不改变，永远指向变量i2；
(c)合法。i是一个常量，r是一个常量引用，绑定到字面值常量0；
(d)合法。p3是一个常量指针，p3的值永不改变，即p3永远指向变量i2；同时p3指向的是常量，即我们不能通过p3改变所指对象的值；
(e)合法。p1指向一个常量，我们不能通过p1改变其所指对象的值；
(f)非法。引用本身不是对象，不能让引用恒定不变。
(g)合法。i2本身是一个常量，r是一个常量引用。