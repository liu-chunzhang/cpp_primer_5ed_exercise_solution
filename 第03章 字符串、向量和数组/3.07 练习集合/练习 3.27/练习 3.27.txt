对于数组维度的要求有两个：一是维度表示数组中元素的个数，因此必须是大于0的整数；二是维度属于数组类型的一部分，在编译时必须是已知的，故必须是一个常量表达式！
(a)非法。buf_size仅仅是一个普通的无符号数，不是常量，不能够作为数组的维度；
(b)合法。
(c)非法。txt_size( )是一个普通的函数调用，没有被定义为constexpr，不能够作为数组的维度。
(d)非法。当使用字符串字面值初始化字符数组时，默认在尾部添加一个空字符'\0'，算上这个字符，该字符串共有12个字符，但是字符数组st的维度只有11，无法容纳题目中的字符串。