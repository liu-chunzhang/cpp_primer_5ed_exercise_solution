指针也是一个对象，与其相关的属性有三个，分别是指针本身的值（一个内存地址值，表示了所指对象在内存中的存储地址），所指的对象（可以通过解引用进行访问的对象）和指针本身在内存中的存储位置（因为指针也是一个对象，也储存在内存的某个位置，它有自己的地址）。
指针的值是其所指的对象的内存地址，如果把两个指针加在一起，就是试图将内存中两个对象的存储地址加在一起，这并没什么实际意义。
与之相反，指针的减法是有意义的：如果两个指针指向了同一个数组的不同元素，则它们相减的结果表征了它们所指的元素在数组中的距离。