(a)元素数量是0；
(b)元素数量是10，每一个元素都被默认初始化为0；
(c)元素数量是10，每一个元素都被直接初始化为42；
(d)元素数量是1，被列表初始化为10；
(e)元素数量是2，被列表初始化为10、42；
(f)元素数量是10，每一个元素都被默认初始化为空串（无法执行列表初始化后，编译器尝试用默认值初始化vector对象）；
(g)元素数量是10，每一个元素都被直接初始化为“hi”（无法执行列表初始化后，编译器尝试用默认值初始化vector对象）。