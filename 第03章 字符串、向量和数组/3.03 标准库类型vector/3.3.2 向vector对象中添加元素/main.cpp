// 本程序用于验证教材中3.3.2节内容的可靠性，并提供感性认知
#include <iostream>
#include <vector>
using namespace std;

int main() {
    /*
    vector<int> v2 ;                     // v2是一个空的vector对象
    for( int i = 0 ; i != 100 ; ++i )
        v2.push_back( i ) ;              // 依次将整数值放到v2尾端
    */

    /*
    string word ;                        // 空vector对象
    vector<string> text ;
    while( cin >> word )
        text.push_back( word );          // 把word添加到text后面
    */

    return 0;
}
