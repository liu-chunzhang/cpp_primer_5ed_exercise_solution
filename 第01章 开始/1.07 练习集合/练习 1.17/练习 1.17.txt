代码同1.4.4节，打开相应cbp文件可以尝试。
如果输入的所有值都相等，则while循环中的else分支永远不会执行，直到输出结束，while循环退出，循环后的输出语句打印这一唯一的一个值和它出现的次数。
若没有重复值，则while循环中if语句的真值分支永远不会执行，每读入一个值，都会进入else分支，打印它的值和出现次数1。输入结束后，while循环退出，循环后的输出语句打印最后一个值和其出现次数1。