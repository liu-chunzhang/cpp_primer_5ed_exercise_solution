对g( 42 )，T被推断为int，val的类型为int&&，因此v是一个vector<int>变量；
假定已定义int型变量i，则对g( i )，T被推断为int&，val的类型折叠为int&，因此v被声明为vector<int&>，不过，vector管理动态内存空间，在里面保存其元素。这就需要维护执行动态内存空间的指针，此指针的类型应该是执行元素类型的指针。但是引用不是对象，没有实际地址，因此不能定义指向引用的指针，因此不能定义指向int&的指针，也就不能声明vector<int&>，从而编译报错！