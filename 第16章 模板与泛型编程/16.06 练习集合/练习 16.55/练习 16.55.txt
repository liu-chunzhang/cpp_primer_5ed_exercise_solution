将非可变参数版本放在可变参数版本之后，也属于”定义可变参数版本时，非可变参数版本声明不在作用域中“的情形，因此可变参数版本将陷入无限递归。但是这里的无限递归并不是指运行时的无限递归调用，而是在编译时递归的包扩展。例如调用print( cout , i , s , 42 )，正常的包扩展过程是：
	print( cout , s , 42 ) ;
	print( cout , 42 ) ;
最后一步与非可变参数版本匹配。但当非可变版本不在作用域时，还会继续扩展为：
	print( cout ) ;
此时无法与任何模板匹配，从而产生编译错误。