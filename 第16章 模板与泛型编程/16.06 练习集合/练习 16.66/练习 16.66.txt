优点：重载的非模板函数在同等匹配条件下的优先级更高，会被优先调用。
缺点：重载会影响函数匹配（即编译器在函数匹配过程中会将新的重载函数作为候选之一来选择最佳匹配，因而需要小心设计，避免实际匹配并非预期）。
特例化不影响函数匹配，它并不是为编译器进行函数匹配提供一个新的选择，而是为模板的一个特殊实例提供不同于原模板的特殊定义，本质是接管了编译器在完成函数匹配后的部分实例化的工作（即当某模板是最佳匹配时，且需要实例化为特殊实例，不再从原模板进行实例化，而是使用这个特例化版本），相比之下特例化更为简单——当某个模板不符合我们的需求时，只需要设计满足需求的特例化模板即可。